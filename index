<!doctype html>
<html lang="ja">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>抽選結果発表</title>
<meta name="robots" content="noindex,nofollow">

<style>
:root{
  --bg1:#060b1f;
  --bg2:#0d1f35;
  --gold:#ffd700;
  --blue:#60a5fa;
  --blue2:#3b82f6;
}
body{
  margin:0;
  font-family:system-ui,-apple-system,"Hiragino Sans","Noto Sans JP";
  min-height:100vh;
  display:grid;
  place-items:center;
  padding:24px;
  overflow:hidden;
  background:
    radial-gradient(900px 500px at 50% 20%, rgba(255,215,0,.25), transparent 60%),
    radial-gradient(900px 500px at 80% 80%, rgba(59,130,246,.25), transparent 60%),
    linear-gradient(180deg,var(--bg1),var(--bg2));
  color:white;
}

.card{
  width:min(760px,95%);
  padding:60px 24px;
  border-radius:22px;
  background:rgba(255,255,255,.06);
  text-align:center;
  box-shadow:0 40px 100px rgba(0,0,0,.6);
}

h1{font-size:32px;margin-bottom:10px;}
.sub{color:#cbd5e1;margin-bottom:30px;}

.count{
  font-size:80px;
  font-weight:900;
  color:var(--gold);
  margin:20px 0;
  text-shadow:0 0 50px rgba(255,215,0,.9);
}

.btn{
  display:inline-block;
  padding:18px 40px;
  border-radius:16px;
  background:linear-gradient(180deg,var(--blue),var(--blue2));
  color:white;
  font-weight:900;
  text-decoration:none;
  font-size:18px;
  box-shadow:0 20px 60px rgba(59,130,246,.5);
  cursor:pointer;
}

.loading{
  margin-top:20px;
  font-size:14px;
  opacity:.7;
  display:none;
}
</style>
</head>

<body>

<div class="card">
  <h1>抽選結果の発表</h1>
  <p class="sub">エントリー情報を確認しました。</p>

  <div class="count" id="count">3</div>

  <a class="btn" id="goBtn">結果を見る</a>

  <div class="loading" id="loading">
    結果を確認中です...
  </div>
</div>

<script>
let c=3;
let el=document.getElementById("count");

let timer=setInterval(()=>{
  c--;
  el.textContent=c;
  if(c<=0){
    clearInterval(timer);
    el.textContent="🎉";
  }
},1000);

document.getElementById("goBtn").addEventListener("click",()=>{
  document.getElementById("loading").style.display="block";
  setTimeout(()=>{
    window.location.href="win.html";
  },1200);
});
</script>

</body>
</html>
